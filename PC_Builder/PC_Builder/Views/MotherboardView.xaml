<UserControl x:Class="PC_Builder.Views.MotherboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PC_Builder.Views"
             xmlns:viewmodels="clr-namespace:PC_Builder.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Opacity="0.9">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,5">
                    <GradientStop Color="#23262B" Offset="0.0"/>
                    <GradientStop Color="#404D5B" Offset="0.4"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <DataGrid x:Name="DGrid" ItemsSource="{Binding Source=Motherboards}" ColumnHeaderStyle="{StaticResource GridHeaderStyle}" BorderBrush="#23262B">
            <DataGrid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,5">
                    <GradientStop Color="#23262B" Offset="0.0"/>
                    <GradientStop Color="#404D5B" Offset="0.4"/>
                </LinearGradientBrush>
            </DataGrid.Background>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Model" Binding="{Binding Manufacturer}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Chipset" Binding="{Binding Chipset}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Socket" Binding="{Binding Socket}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Form factor" Binding="{Binding Form_factor}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Memory type" Binding="{Binding Ram_type}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" IsReadOnly="True" Width="*"/>
                <DataGridTemplateColumn Header="a" IsReadOnly="True" Width="auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <Button x:Name="BtnOpen" Cursor="Hand"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
